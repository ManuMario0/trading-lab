[package]
name = "my_strategy"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
trading = { path = "../../trading-api" }
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
